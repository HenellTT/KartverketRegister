@{
    ViewData["Title"] = "Flight Mode";
}

<!-- Leaflet Map Container -->
<div class="BigContainer">

    <div class="topBar">
        <div class="topBar-left"></div>
        <div class="topBar-center">
            <button class="btn-point" onclick="PFM.SetMode('Marker')">Point</button>
            <button class="btn-line" onclick="PFM.SetMode('Line')">Line</button>
        </div>
        <div class="topBar-right">
            <button class="btn-exit" onclick="location.href='/Pilot'">Exit</button>
        </div>
    </div>

    <!-- bottom buttons -->
    <div class="bottom-left">
        <button class="btn-theme" onclick="dms()">Theme</button>
    </div>

    <div class="bottom-center">
        <button class="btn-revert" onclick="PFM.RevertLastAction()">Revert</button>
        <button class="btn-clear" onclick="PFM.ClearActions()">Clear</button>
    </div>

    <div class="bottom-right">
        <button class="btn-submit" onclick="formElement.requestSubmit()">Submit</button>
    </div>

    <div id="map"></div>
</div>

<form id="tempMarkerFormForm" style="display:none;">
    @Html.AntiForgeryToken()
    <input type="text" id="type" name="type" placeholder="Enter type">

    <input type="text" id="description" name="description" placeholder="Enter description">

    <input type="number" id="lat" name="lat" readonly>

    <input type="number" id="lng" name="lng" readonly>

    <input step=0.01 type="number" id="height" name="height" placeholder="Enter height">
    <input type="text" id="geojson" name="geojson">


    <button type="submit">Save Marker</button>
</form>


<link rel="stylesheet" href="~/css/MapPage-TempMarkerForm.css" asp-append-version="true" />
<link rel="stylesheet" href="~/css/FlightMode.css" asp-append-version="true" />
<script src="~/js/PilotFlightMode.js"></script>
<script src="~/js/icons.js"></script>
<script src="~/js/ObstacleMapLoader.js"></script>

<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
      integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
      crossorigin="" />
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
        integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
        crossorigin=""></script>

<script>
    // Initialize the map
    map = L.map('map').setView([51.505, -0.09], 13);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
    }).addTo(map);

    let PFM;
    let OML;

    fetch("/icon/geticons").then(res => res.json()).then((IconList) => {
        Window.icons = new Icons(IconList);
        PFM = new PilotFlightMode(map, L);
        PFM.Init();
        OML = new ObstacleMapLoader(map, L);
        OML.Init();
    });

    const formElement = document.getElementById('tempMarkerFormForm');

    formElement.addEventListener('submit', function (e) {
        e.preventDefault();
        const last = PFM.ActionList[PFM.ActionList.length - 1];
        formElement.lat.value = last.lat;
        formElement.lng.value = last.lng;
        formElement.geojson.value = PFM.ExportGeoJsonString();
        formElement.height.value = PFM.GetHeight().toFixed(2);
        formElement.type.value = "Fill in";
        formElement.description.value = prompt('Brief Description / Note for yourself');

        const formData = new FormData(formElement);

        fetch('/Tempmarker/SubmitMarker', {
            method: 'POST',
            body: formData
        })
        .then(response => {
            if (response.ok) return response.text();
            throw new Error('Network response was not ok');
        })
        .then(data => {
            console.log('Server response:', data);
            formElement.reset();
        })
        .catch(error => {
            console.error('Error:', error);
        });

        PFM.ClearActions();
    });
</script>